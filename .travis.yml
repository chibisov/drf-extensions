language: python

python:
  - "3.5"
  - "3.4"
  - "2.7"

install:
  - pip install tox

env:
  - TOX_ENV=latest3.5,
  - TOX_ENV=latest3.4,
  - TOX_ENV=latest2.7,
  - TOX_ENV=django.1.8.lts,
  - TOX_ENV=django.1.7

script:
  - tox -e $TOX_ENV -- tests_app
