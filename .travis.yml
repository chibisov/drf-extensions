language: python

python:
  - "2.7"
  - "3.3"

env:
  - DJANGO="django==1.5.1 --use-mirrors"

install:
  - pip install $DJANGO
  - pip install -e git+https://github.com/tomchristie/django-rest-framework.git#egg=djangorestframework

script:
  - python rest_framework_extensions/runtests/runtests.py
